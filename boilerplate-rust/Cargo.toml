[package]
name = "boilerplate-rust"
version = "0.1.0"
edition = "2021"

[profile.release]
opt-level = "z"     # Optimize for size
lto = true          # Link-time optimization
panic = "abort"     # Abort on panic (no unwinding tables)
strip = true        # Strip symbols
